<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Vista Test Mustache</title>
    {{{head}}}
    <!-- Bootstrap CSS -->
</head>
<body class="bg-light text-dark">
    <main class="container py-5">
        <header class="d-flex align-items-center justify-content-between mb-4">
            <h1 class="h2 fw-bold">Vista Test (Mustache .mst)</h1>
        </header>
        <section aria-label="Valor recibido">
            <p class="mb-4 fs-5">
                Valor recibido por URL:
                <strong class="text-primary">{{valor}}</strong>
            </p>
        </section>
        <section aria-label="Valor recibido por POST">
            <p class="mb-4 fs-5">
                Valor recibido variable usario:
                {{#usuario}}
                    <strong class="text-success">{{nombres}}</strong>
                {{/usuario}}
            </p>
        </section>
        <section class="mt-4" aria-label="Tabla de datos">
            <div class="table-responsive rounded shadow">
                <table id="tablatest" class="table table-bordered table-hover mb-0">
                    <thead class="table-primary">
                        <tr>
                            <th scope="col" class="fw-bold">#</th>
                            <th scope="col" class="fw-bold">Valor</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#datos}}
                        <tr>
                            <td>{{numero}}</td>
                            <td>{{valor}}</td>
                        </tr>
                        {{/datos}}
                    </tbody>
                </table>
            </div>
        </section>
    </main>
    <!-- Bootstrap JS (optional, for some components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
